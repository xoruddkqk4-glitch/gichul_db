# -*- coding: utf-8 -*-
# 05-gichul_db 런타임 의존성 (Python 3.11 기준, 2026-09-22 실측 버전 고정)
# 설치: pip install -r requirements.txt
# 주의: 첫 줄의 coding 선언은 Windows(cp949) pip이 한글 주석을 UTF-8로 읽게 하는 용도이므로 삭제 금지

# 웹 서버
fastapi==0.141.1
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.32   # FastAPI Form()/File() 업로드 처리에 필요

# 문서 파싱
pymupdf==1.28.2            # PDF 텍스트/이미지 추출 (pdf_parser.py, hwp_parser.py)
pillow==12.2.0             # 정답표 이미지 처리 (pdf_parser.py)
pyhwpx==1.7.2; sys_platform == "win32"   # 한컴 HWP COM 자동화 (hwp_parser.py)
pywin32==312; sys_platform == "win32"    # pyhwpx 의존 + Windows COM

# 개발/테스트
pytest==9.1.1             # 회귀 테스트 (tests/)

# 음성 합성 (TTS)
edge-tts>=7.2.8           # Microsoft Neural 무료 음성 합성 (영어 듣기 평가 M/W 듀얼 보이스)
